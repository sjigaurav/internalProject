/*
 * A sample implementation of the dynamic participant chooser API.
 * 
 * This implementation evaluates the initiator of a workflow instance as the
 * dynamic participant.
 * 
 */
function getParticipant() {
	return workItem.getWorkflow().getInitiator();
}